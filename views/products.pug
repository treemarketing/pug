html
    head
        title='test pug'
        style
            include styles.css
    body
        h1=title
        div
        for item in products
            div
                p=item.id
                p=item.title
                p(class="p-price")=item.price
                img(class='product-img', src=item.thumbnail)
    button#formulario(onclick="window.location.href='http://localhost:8080/form';") volver al formulario
    
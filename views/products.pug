html
    head
        title='test pug'
        style
            include styles.css
    body
        h1=title
        div
        for item in products
            div
                p=item.id
                p=item.title
                p(class="p-price")=item.price
                img(class='product-img', src=item.thumbnail)